# Mediu de test — SQLite pentru rulare rapidă, izolată, fără server extern.
# Schema producției este PostgreSQL (vezi migrations/ + docker-compose).
APP_SECRET=test-secret
DATABASE_URL="sqlite:///%kernel.project_dir%/var/test.db"
MESSENGER_TRANSPORT_DSN=in-memory://
KERNEL_CLASS='App\Kernel'
